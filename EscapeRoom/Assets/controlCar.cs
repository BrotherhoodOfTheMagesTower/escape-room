using UnityEngine;

public class controlCar : MonoBehaviour
{
    //GameObject player = GameObject.FindWithTag("Player");
    //GameObject movingCar = GameObject.FindWithTag("movingCar");
    //public Camera carCamera;
    //public Camera mainCamera;
    //Camera carCamera = Camera.("carCamera");
    // Start is called before the first frame update


    void Start()
    {
        //if (carCamera == null)
        //    carCamera = GameObject.FindGameObjectWithTag("movingCarCamera").GetComponent<Camera>();
        //else
        //{
        //    carCamera.enabled = true;

        //}
        //if (mainCamera == null)
        //    mainCamera = GameObject.FindGameObjectWithTag("MainCamera").GetComponent<Camera>();
        //else
        //{
        //    mainCamera.enabled = false;
        //}
    }

    // Update is called once per frame
    void Update()
    {

        //carCamera.enabled = true;
        //mainCamera.enabled = false;

    }
}
